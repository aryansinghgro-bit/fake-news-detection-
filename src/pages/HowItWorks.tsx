import { MODEL_STEPS } from '@/constants';
import { ArrowDown, Braces, FileText, GitBranch, ScanText, Sigma } from 'lucide-react';
const icons = [FileText, ScanText, Sigma, GitBranch, Braces];
export function HowItWorks() { return <main className="page shell"><div className="page-heading centered"><p className="eyebrow">The methodology</p><h1>From words to <em>signal.</em></h1><p>Five transparent stages turn a news article into a model-based classification.</p></div><div className="timeline">{MODEL_STEPS.map((step, index) => { const Icon = icons[index]; return <div className="timeline-step" key={step.number}><div className="step-number">{step.number}</div><div className="step-icon"><Icon size={20} /></div><div><p className="eyebrow">{step.label}</p><h2>{step.description.split(' ').slice(0, 4).join(' ')}</h2><p>{step.description}</p></div>{index < MODEL_STEPS.length - 1 && <ArrowDown className="step-arrow" size={18} />}</div>; })}</div><div className="method-note"><p className="eyebrow">Important distinction</p><h2>This is classification — not fact-checking.</h2><p>The model has no access to a universal truth database. It recognizes linguistic patterns associated with its labeled training examples and communicates the strength of that match.</p></div></main>; }
