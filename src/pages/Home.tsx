import { ArrowRight, Check, Database, LockKeyhole, Sparkles } from 'lucide-react';
import { useNavigate } from 'react-router-dom';
import { Hero } from '@/components/Hero';
import { ModelStats } from '@/components/ModelStats';

export function Home() {
  const navigate = useNavigate();
  return <><Hero /><section className="shell home-section"><div className="section-intro"><div><p className="eyebrow">A clearer signal</p><h2>Built for thoughtful<br /><em>analysis.</em></h2></div><p>TruthScan turns a long-form article into a concise, explainable signal — without pretending that confidence is the same thing as truth.</p></div><div className="feature-grid"><article className="feature-card feature-card-wide"><div className="feature-icon"><Sparkles /></div><h3>Pattern recognition, not verdicts.</h3><p>A TF-IDF vectorizer and Logistic Regression model examine language patterns learned during training.</p><button className="text-link" onClick={() => navigate('/how-it-works')}>Explore the pipeline <ArrowRight size={15} /></button></article><article className="feature-card"><div className="feature-icon green"><LockKeyhole /></div><h3>Private by default.</h3><p>Your recent analyses stay in this browser. Article text is not stored by the interface.</p></article><article className="feature-card"><div className="feature-icon blue"><Database /></div><h3>Measured performance.</h3><p>See the evaluation metrics generated by the trained model, not invented product claims.</p></article></div></section><section className="metrics-band"><div className="shell"><div className="section-intro compact"><div><p className="eyebrow">Under the hood</p><h2>Model <em>performance</em></h2></div><p>Evaluation metrics are loaded from the backend after training.</p></div><ModelStats /></div></section><section className="shell note-section"><div className="note-card"><div className="note-mark"><Check /></div><div><p className="eyebrow">Use with context</p><h3>Confidence describes the model, not reality.</h3><p>A high score means the classifier recognized a familiar pattern in its training data. It does not establish whether an article is objectively true or false.</p></div></div></section></>;
}
